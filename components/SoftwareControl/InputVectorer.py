

class InputVectorer:
    """
    Takes 3 axes from -1 to 1 and a set (of transformations)
    Outputs an XYR Vector
    """

    def inputVector(self, axis1, axis2, axis3, set):
        """
        Takes 3 axes and a set of transformations
        Applies the transformations and outputs an XYR Vector
        """
